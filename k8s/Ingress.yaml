apiVersion: v1
kind: Ingress
metadata:
  name: springboot-redis-service
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  selector:
    app: springboot-redis
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
    