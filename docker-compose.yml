
services:
  redis:
    image: redis
    ports:
      - "6379:6379"


  java:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: springredisapp
    ports:
      - "8080:8080"
    depends_on:
      - redis
